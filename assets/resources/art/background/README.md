# background/

Drop PNGs here. Filenames must match `AssetConfig.background.*.path`:

- `field.png`  → `art/background/field`
- `ground.png` → `art/background/ground`

Leave empty to keep the Graphics-color placeholders.
